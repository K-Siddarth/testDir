#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    /*
     * Verify that a graph is eulerian or not
     * Start from a node and do dfs, while doing dfs.
     * while dfs is going on, make sure to delete edges and add the appropriate node to the euler tour string
     * MAKE SURE THAT DELETING AN EDGE DOES NOT DISCONNECT THE GRAPH INTO 2 SETS OF EDGES   
     */

    return 0;
}
